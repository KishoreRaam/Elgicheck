GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-3.5-flash-lite

# Optional. Leave unset to run eligibility checks in MOCK mode (deterministic, offline).
# Set to a real Stedi API key to call the live Eligibility Check API.
STEDI_API_KEY=
